import React from 'react';
import FuelImage from './_Component/FuelImage';

export const metadata = {
  title: 'Upload Image',
  description: 'Admin upload image page',
};

const page = () => {
  return (
    <div>
      <FuelImage />
    </div>
  );
};

export default page;
